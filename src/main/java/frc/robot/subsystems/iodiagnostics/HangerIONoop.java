package frc.robot.subsystems.iodiagnostics;

/**
 * No-op HangerIO — used in simulation or when hanger hardware is absent.
 * All methods are inherited as no-ops from the interface defaults.
 * Inputs remain at their zero-initialized values every cycle.
 */
public class HangerIONoop implements HangerIO {}
